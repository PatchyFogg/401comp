# 401comp.com

Source for the 401comp.com software portfolio site — a single static HTML/CSS page cataloging desktop apps. Deployed via GitHub Pages; `CNAME` points it at the custom domain.

To preview locally, just open `index.html` in a browser — no build step.
